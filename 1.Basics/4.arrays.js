const arr = ["Annelien", "Yannick", "Marie", "Kevin", "Charlie"];
//console.log(arr.length); //6
//console.log(arr); //['Annelien', 'Yannick', 23, false]
//console.log(arr[arr.length-1]) //false
for (let i = 0; i < arr.length; i++) {
  console.log(`  - ${arr[i]}`);
}
